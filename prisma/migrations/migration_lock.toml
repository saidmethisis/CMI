# Фиксирует СУБД, под которую написаны миграции.
# Без этого файла Prisma не может определить провайдера каталога миграций:
# защита от случайной смены базы не работает, и `migrate diff --from-migrations`
# (штатная проверка расхождений на сервере) завершается ошибкой.
provider = "postgresql"
